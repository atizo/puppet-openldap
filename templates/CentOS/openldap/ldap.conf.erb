# Manged by puppet
# User changes will be destroyed the next time puppet is run.
# If this file is changed, please update it on the puppet side as well!
#
# LDAP Defaults
#

# See ldap.conf(5) for details
# This file should be world readable but not world writable.

#BASE   dc=example, dc=com
#URI    ldap://ldap.example.com ldap://ldap-master.example.com:666

#SIZELIMIT      12
#TIMELIMIT      15
#DEREF          never
URI ldap://<%= real_ldap_server %>
BASE <%= real_ldap_base %>
TLS_CACERTDIR /etc/openldap/cacerts
